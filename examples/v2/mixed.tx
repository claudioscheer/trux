package main

func step(value float) float {
    return value + 1.25
}

func main() int {
    let i int = 0
    let total float = 0.0

    while i != 3 {
        total = step(total)
        i = i + 1
    }

    print(total)
    print(i == 3, " ", total > 3.0)

    return 0
}
