# IO Basics

This project uses stdin, text file IO, and flat CSV IO in one small program.

Run from the repository root:

```bash
printf 'Ada\n41\n' | trux run examples/projects/io_basics/main.tx
```
