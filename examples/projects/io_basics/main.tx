package main

func main() int {
    let name string = read_line()
    let bonus int = read_int()

    let note string = read_file("examples/projects/io_basics/input.txt")
    write_file("examples/projects/io_basics/copy.txt", note + "\n" + name)

    let cells list[string] = read_csv("examples/projects/io_basics/input.csv", 2)
    append(cells, name)
    append(cells, "interactive")
    write_csv("examples/projects/io_basics/output.csv", cells, 2)

    print("hello ", name)
    print(bonus + 1)
    print(len(cells), " ", cells[0], " ", cells[1])

    return 0
}
