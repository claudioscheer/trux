package main

func mean(a float, b float) float {
    return (a + b) / 2.0
}

func main() int {
    let x float = mean(1.5, 2.5)
    let y float = x * 3.0 - 1.0

    print(x)
    print(y)

    if y >= 5.0 {
        print("high")
    } else {
        print("low")
    }

    return 0
}
