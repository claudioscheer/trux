package main

func main() int {
  let seed string = readFile("examples/projects/file_notes/seed.txt")
  let extra string = readLine()
  let summary string = seed + "\n" + extra

  writeFile("examples/projects/file_notes/summary.txt", summary)
  print(summary)

  return 0
}
