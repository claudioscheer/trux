package main

import "io"

func main() int {
  let seed string = io.readFile("examples/projects/file_notes/seed.txt")
  let extra string = io.readLine()
  let summary string = seed + "\n" + extra

  io.writeFile("examples/projects/file_notes/summary.txt", summary)
  print(summary)

  return 0
}
