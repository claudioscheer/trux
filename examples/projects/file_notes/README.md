# File Notes

Reads a seed note, combines it with one line of stdin, and writes the result to `summary.txt`.

```bash
printf 'review IO examples\n' | trux run examples/projects/file_notes/main.tx
```
