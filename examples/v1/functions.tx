package main

func project() string {
    return "trux"
}

func stable() bool {
    return false
}

func main() int {
    let name string = project()
    let isStable bool = stable()

    print(name, " ", 1, " ", isStable)
    print(isStable)

    return 0
}
