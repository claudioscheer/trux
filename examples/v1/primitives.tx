package main

func main() int {
    let name string = "trux"
    let ready bool = true
    let count int = 3
    let escaped string = "line\nquote: \""

    print(name)
    print(ready)
    print(count)
    print(escaped)

    return 0
}
