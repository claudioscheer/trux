package main

import "io"

func main() int {
  let name string = io.readLine()
  let start int = io.readInt()

  print("counter for ", name)
  for let i int = 0; i < 3; i = i + 1 {
    print(start + i)
  }

  return 0
}
